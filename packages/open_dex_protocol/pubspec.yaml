name: open_dex_protocol
description: Versioned wire protocol for DroidPier.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  open_dex_api:
    path: ../open_dex_api

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
